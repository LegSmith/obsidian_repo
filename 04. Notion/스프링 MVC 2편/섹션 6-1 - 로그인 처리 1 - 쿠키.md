## 로그인 요구사항

---

- 홈 화면 - 로그인 전
    - 회원가입 , 로그인
- 홈 화면 - 로그인 후
    - 본인 이름( ex : 누구님 환영합니다)
    - 상품 관리, 로그 아웃
    - 회원가입, 상품관리
- 보안 요구사항
    - ==**로그인 사용자만 상품에 접근,관리**==
    - 로그인하지 앉은 사용자는 상품목록 접근자체가 안됨. → 로그인 화면으로 이동

## 회원 가입 구현

---

### 테스트 데이터 입력

- 상품목록 데이터2개 선입력
- 회원1명 생성 후 저장
- `@PostConstruct`_**어노테이션이 붙은 메서드는 애플리케이션을 실행하고 바로 실행하는 메서드**_

    ```java
    @PostConstruct
    public void init() {
        itemRepository.save(new Item("itemA", 10000, 10));
        itemRepository.save(new Item("itemB", 20000, 20));

        Member member = new Member();

        member.setLoginId("ronaldo");
        member.setPassword("1234");
        member.setName("Cristiano");

        memberRepository.save(member);
    }
    ```


### Member 엔티티 생성

- Validatino 이 제공하는 검증 어노테이션을 사용하여 엔티티 생성

    > 공부용이기 때문에 `@Data` 사용 → `@Getter` , `@Setter` 권장(가급적이면 @Setter 도 비추)

    ```java
    @Data
    public class Member {

        private Long id;

        @NotEmpty
        private String loginId; // 로그인 ID
        @NotEmpty
        private String name; // 사용자 이름
        @NotEmpty
        private String password;
    }
    ```


### MemberRepository 생성

- 따로 DB 가 없기 때문에 Map 컬렉션을 static으로 선언하여 DB 처럼 사용
- ID 값도 sequence 변수를 static 으로 생성
- ==**Optional 은 자바8 기술이고 Null 값이 있는 경우를 대비해서 객체를 Optinal 로 감싸준다.**==

    ```java
    @Slf4j
    @Repository
    public class MemberRepository {

        private static Map<Long, Member> store = new HashMap<>(); // static 사용
        private static long sequence = 0L; //static 사용

        public Member save(Member member){
            member.setId(++sequence);
            log.info("save : member={}", member);
            store.put(member.getId(),member);

            return member;
        }

        public Member findById(Long id){
            return store.get(id);
        }

        public Optional<Member> findByLoginId(String loginId){
            /*List<Member> all = findAll();
            for (Member member : all) {
                if(member.getLoginId().equals(loginId)){
                    return Optional.of(member);
                }
            }
            return Optional.empty();*/

            return findAll().stream()
                    .filter(m -> m.getLoginId().equals(loginId))
                    .findFirst();
        }

        public List<Member> findAll(){
            return new ArrayList<>(store.values());
        }

        public void clearStore(){
            store.clear();
        }
    }
    ```


### MemberController 생성

- `@Valid` 를 이용하여 ==**객체 오류 검증 후 데이터 저장후 홈화면으로 redirect**==

    ```java
    @Controller
    @RequiredArgsConstructor
    @RequestMapping("/members")
    public class MemberController {
        private final MemberRepository memberRepository;

        @GetMapping("/add")
        public String addForm(@ModelAttribute("member") Member member){
            return "members/addMemberForm";
        }

        @PostMapping("/add")
        public String save(@Valid @ModelAttribute Member member, BindingResult bindingResult){
            if(bindingResult.hasErrors()){
                return "members/addMemberForm";
            }

            memberRepository.save(member);

            return "redirect:/";
        }
    }
    ```


## 로그인 구현

---

### LoginService

- `loginId` 와 `password` 를 파라미터로 받는 `login()` 메서드에서 `loginId` 를 통해 **==엔티티를 찾고 엔티이의 비밀번호와 파라미터로 받은 비밀번호를 비교하여 다를 경우 null 반환==**

    ```java
    @Service
    @RequiredArgsConstructor
    public class LoginService {
        private final MemberRepository memberRepository;

        /**
         *
         * @param longinId
         * @param password
         * @return null 로그인 실패
         */
        public Member login(String longinId, String password){
            return memberRepository.findByLoginId(longinId)
                    .filter(m->m.getPassword().equals(password))
                    .orElse(null);
        }
    }
    ```


### LoginController

- `LoginForm` 이라는 ==**DTO를 컨트롤러에서 받고 오류검증 후 Member 엔터티로 영속화**==
- 영속화된 Member 엔터티가 비어있는지 체크해서 비어있다면 `reject()` 메서드로 ==**글로벌 오류 처리**==

    ```java
    @Slf4j
    @Controller
    @RequiredArgsConstructor
    public class LoginController {
        private final LoginService loginService;

        @GetMapping("/login")
        public String loginForm(@ModelAttribute("loginForm") LoginForm form){
            return "login/loginForm";
        }

        @PostMapping("/login")
        public String login(@Valid @ModelAttribute LoginForm form, BindingResult bindingResult){
            if(bindingResult.hasErrors()){
                return "login/loginForm";
            }
            Member loginMember = loginService.login(form.getLoginId(), form.getPassword());

            if(loginMember == null){
                bindingResult.reject("loginFail","아이디 또는 비밀번호가 맞지 않습니다.");
                return "login/loginForm";
            }

            // 로그인 성공 처리 TODO

            return "redirect:/";
        }
    }
    ```


## 로그인/로그아웃 - 쿠키사용

---

### LoginController

- ==**Cookie 객체를 생성**==하고 memberId 라는 이름과 login된 멤버의 id값을 value 로 담아서 생성한다.
- ==**HttpServletResponse 를 이용하여 response 헤더 cookie를 담아서 응답**==

    ```java
    @PostMapping("/login")
    public String login(@Valid @ModelAttribute LoginForm form, BindingResult bindingResult, HttpServletResponse response){
    		// 생략 ...

        // 로그인 성공 처리(세션 쿠키) -> 브라우저 종료 시 모두 종료
        Cookie idCookie = new Cookie("memberId", String.valueOf(loginMember.getId()));
        response.addCookie(idCookie);

        return "redirect:/";
    }
    ```


### HomeController

- 기존에는 로그인 구분없이 홈화면을 띄워줬지만 이제는 로그인을 구분하여 홈화면을 뿌려준다.
- 스프링에서 제공하는 `@CookieValue` 어노테이션을 이용하여 쿠키에 담겨있는 memberId 를 파라미터로 받는다.

    - name 속성은 쿠키이름, **==required 는 false 로 선언하여 로그인되지 않은 사용자도 해당 컨트롤러로 들어올 수 있게 한다.==**

    ```java
    @GetMapping("/")
    public String homeLogin(@CookieValue(name="memberId",required = false) Long memberId,
                            Model model){
        if(memberId == null)
            return "home";

        // 로그인
        Member loginMember = memberRepository.findById(memberId);
        if(loginMember == null)
            return "home";

        model.addAttribute("member",loginMember);

        return "loginHome";
    }
    ```


### Logout

- **==저장된 쿠키의 이름과 같고 생성시간이 0인 쿠키를 생성==**하여 응답헤더에 담아서 응답해주면 쿠키가 바뀌면서 ==**종료날짜가 0이기 때문에 로그아웃**==이 된다.

    ```java
    @PostMapping("/logout")
    public String logout(HttpServletResponse response){
        expireCookie(response, "memberId");

        return "redirect:/";

    }

    private static void expireCookie(HttpServletResponse response, String cookieName) {
        Cookie cookie = new Cookie(cookieName, null);
        cookie.setMaxAge(0);
        response.addCookie(cookie);
    }
    ```


## 쿠키와 보안 문제

---

### 쿠키 값은 임의로 변경 가능하다.

- 웹 브라우저에서 ==**개발자 모드 → Application → Cookie 메뉴로 가변 임의로 쿠키값이 변경가능**==하다.

### 쿠키에 보관된 정보는 훔쳐갈 수 있다.

- ==**쿠키 값에 개인정보나 신용정보가 있으면 해커가 악이용**==해서 정보를 도용할 수 있다.

### 대안

- 쿠키에 중요한 값을 노출하지 않는다 !
- 서버에서 토큰과 사용자 Id를 매핑해서 인식하여 서버에서 토큰을 관리
- 해커가 토큰을 털어가도 시간이 지나면 사용할 수 없도록 만료시간을 짧게 설정

## 관련 문서

- 상위 목차: [[스프링 MVC 2편]]
- 이전 문서: [[섹션 5 - 검증2 - Bean Validation]]
- 다음 문서: [[섹션 6-2 - 로그인 처리 1 - 세션]]
