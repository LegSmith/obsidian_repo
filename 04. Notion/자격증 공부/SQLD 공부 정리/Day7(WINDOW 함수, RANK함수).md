# Window 함수

---

==**레코드(행) 사이의 관계를 쉽게 정의하기 위한 함수**==

- `WINDOW_FUNCTION` : 윈도우 함수
- `ARGUMENTS` : 인수(컬럼명 등 함수의 작업이 이뤄지는 대상)
- `PARTITION BY` : 테이블의 레코드들을 쪼개는 기준
- `ORDER BY` : 쪼개진 레코드들 내에서 혹은 전체 테이블에서 레코드들을 어떤 기준으로 정렬할지
- `WINDOWING` : 함수의 연산 대상이 되는 레코드의 범위를 정함

    ```sql
    SELECT WINDOW_FUNCTION(ARGUMENTS) 
    OVER([PARTITION BY 컬렴명][ORDER BY 컬럼명][WINDOWING])
    FROM 테이블명;
    ```


### WINDOW_FUNCTION 종류

- 그룹 내 집계함수 : `COUNT` , `SUM` , `MIN` , `MAX` , `AVG` 등
- 그룹 내 순위(RANK) 함수
- 그룹 내 비율 관련 함수
- 그룹 내 행 순서 함수

## Window 함수 - 그룹 내 RANK 함수

- `RANK`
    - 동일한 순위에 대해 ==**동일한 순위**==를 부여한다.
    - ==**동일한 순위를 하나의 건수로 계산하지 않는다. → 1위, 2위, 2위, 4위**==
- `DENSE_RANK`
    - 동일한 순위에 대해 ==**동일한 순위**==를 부여한다.
    - ==**동일한 순위를 하나의 건수로 계산한다. → 1위, 2위, 2위, 3위**==
- `ROW_NUMBER`
    - ==**동일한 순위에 대해 고유한 순위를 부여한다. → 1위, 2위, 3위, 4위**==

## Window 함수 - 그룹 내 비율 함수

- `PERCENT_RANK`
    - 값이 아닌, ==**순서를 대상**==으로, 파티션 내에서의 **순서별 백분율**을 조회함
- `NTILE(n)`
    - 파티션별로 전체 건수를 n 등분한 값을 반환한다.
    - n = 4 이면 4등분한 것 내에서 몇인지 조회
- `CUME_DIST`
    - 파티션 내 전체에서 현재 행의 값 이하인 레코드 건수에 대한 누적 백분율을 조회
    - 누적 분포 상에 0~1 값을 가짐

## Window 함수 - 그룹 내 행 순서 함수

- `FIRST_VALUE`
    - 파티션 내에서 ==**가장 처음 나오는 값**== 반환(`MAX` 와 동일)
- `LATS_VALUE`
    - 파티션 내에서 ==**가장 마지막에 나오는 값**== 반환(MIN 과 동일)
- `LAG(컬러명, 레코드위치 차이값)`
    - ==**이전 행**==을 가져온다.
- `LEAD(컬럼명, 레코드위치 차이값, null일 경우 대체값)`
    - ==**다음(특정 위치의) 행**==을 가져온다.
    - defalut 는 1이다.

![[Untitled 57.png|Untitled 57.png]]

- LAG는 뒤로 가는 함수이고 매장코드 기준 위로 2번 올라가는 행의 매장코드를 말한다. 우선 ORDER BY를 하면 아래와 같다.
- 매장코드가 C 기준에서 위로 2행의 매장코드가 들어가기 때문에 ㄱ 은 E가 들어온다.

    |매장코드|지역명|
    |---|---|
    |E|부산|
    |D|부산|
    |C|부산|
    |B|부산|
    |A|서울|

- 그리고 LEAD는 아래로 가는 함수이다. 그리고 지역명을 기준으로 PARTITION을 하고 ORDER BY를 하면 다음과 같다.
- 아래로 2행의 매장코드이기 때문에 ㄴ 은 E 가 들어온다.

    |매장코드|지역명|
    |---|---|
    |A|서울|
    |B|부산|
    |C|부산|
    |D|부산|
    |E|부산|


## WINDOWING

==**함수의 연산 대상이 되는 레코드의 범위를 정한다.**==

- `RAGE`
    - ex) `RANGE BETWEEN 100 AND 200`
    - 범위를 지정해줄 때 사용
- `BETWEEN a AND b`
    - a 부터 b 까지 윈도우가 적용됨
- `UNBOUNDED PRECEDING`
    - 윈도우의 시작 위치 = 첫 번째 행
- `UNBOUNDED FOLLOWING`
    - 윈도우의 마지막 위치 = 마지막 행
- `CURRENT ROW`
    - 윈도우 시작 위치가 현재 행


## 테이블 파티션

==**대용량의 테이블을 여러개의 데이터 파일에 분리하여 저장하는 것.**==

### 장점)

1. _**물리적**_으로 분리된 데이터 파일에 저장되어 ==**입력/수정/삭제/조회 성능이 향상**==된다.
2. **==독립적 관리==**가 가능하다
3. ==**조회의 범위를 줄이는 효과**==가 있어서 성능이 향상됨

### 파티션의 종류

- `RANGE PARTITION`
    - ==**값의 범위를 기준**==으로 파니션을 나눠 저장
    - ex) 매출액이 50000 이상인 레코드와 미만인 레코드를 별도로 저장
- `LIST PARTITION`
    - ==**특정 값을 기준**==으로 분할
    - ex) 특정 값이 100일 때와 250일 때 각각 다른 데이터파일에 저장
- `HASH PARTITION`

    - ==**데이터베이스 관리 시스템이**== ==**자체적**====**으로 해시함수**==를 사용해 분할하고 관리하는 방식

## 관련 문서

- 상위 목차: [[SQLD 공부 정리]]
- 이전 문서: [[Day6(JOIN심화,서브쿼리,계층형조회)]]
- 다음 문서: [[Day8(옵티마이저)]]
