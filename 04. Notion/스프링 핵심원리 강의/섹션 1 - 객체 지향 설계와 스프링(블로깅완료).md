  

# 스프링 역사

> EJB → Hibernate → JPA

스프링 이름 유래==**J2EE(EJB)라는 겨울을 넘어 새로운 시작이라는 뜻 ( 봄 : Spring )**==

---

# 스프링 이란 ?

### 필수

- 스프링 프레임워크
- 스프링 부트

### 선택

- 스프링 데이터(JPA)
- 스프링 세션
- 스프링 시큐리티
- 스프링 RestDocs
- 스플이 배치
- 스프링 클라우드

## 스프링 프레임워크

- **핵심 기술 :** ==**스프링 DI 컨테이너, AOP , 이벤트 , 기타**==
- 웹 기술 : ==**스프링 MVC**== , 스프링 WebFlux
- 데이터 접근 기술 : 트랜잭션, JDBC, ORM 지원 , XML 지원

## 스프링 부트

- **==스프링을 편리하게 사용할 수 있도록 지원, 최근에는 기본으로 사용==**
- ==_**Tomcat 을 내장**_==하기 때문에 별도의 웹 서버를 설치하지 않아도 됨
- 손쉬운 빌드 구성을 위한 ==_**starter 종속성 제공**_== → 라이브러리를 가져올 때 한번에 가져옴
- ==_**3rd parth(외부) 라이브러리 자동 구성 → 버전 별로 필요한 라이브러리 자동으로 가져옴**_==
- 관례에 의한 간결한 설정

### Spring을 ==**왜**== 만들었을까 ?

- 스프링은 ==_**자바 언어 기반의 프레임워크**_==
- ==**객제 지향 언어가 가진 강력한 특징을 살려주는 프레임워크**==

---

(여기까지 블로깅)

# 좋은 OOP 란 ?

**유연하고, 변경에 용이해야 한다.**

## 다형성 ( Polymorphism )

![[Untitled 43.png|Untitled 43.png]]

- ==_**역할과 구현으로 구분**_==하면 코드가 유연해지며 변경도 편리해진다.
- **역할 = 인터페이스**
- **구현 = 인터페이스를 구현한 클래스, 구현 객체**

### 스프링과 객체 지향

- _==**스프링은 다형성을 극대화**==_해서 이용할 수 있게 도와준다.
- 스프링은 ==**제어의 역전(IoC), 의존관계 주입(DI)**==은 다형성을 활용할 수 있게 지원해준다.

---

# OOP 의 5가지 원칙(SOLID)

**클린코드로 유명한 로버트 마틴이 좋은 객체 지향 설계의 5가지 원칙**

## SRP 단일 책임 원칙(Single Responsibility Principle)

- ==**한 클래스는 하나의 책임**==만 가져야 한다.
- _==**코드를 변경할 때 파급이 적어야 합니다.**==_

## OCP 개방-폐쇄 원칙(Open/Closed Principle)

- SW 요소는 ==**확장에는 열려 있으나 변경에는 닫혀 있어야 한다.**==
- _==**다형성을 활용**==_하면 된다.
- 하지만 다형성을 사용해도 OCP 원칙을 지킬 수 없다.

## LSP 리스코프 치환 원칙(Liskov Substitution Principle)

- ==**인터페이스의 규약을 지켜야한다.**==
- 단순히 컴파일에 성공하는것에 맞추면 안된다.
- ==**예) 자동차 엑셀 기능인데 뒤로가게 구현하면 컴파일에는 문제가 없지만 LSP 원칙을 위배한다.**==

## ISP 인터페이스 분리 원칙(Interface Segregation Principle)

- 특정 클라이언트를 위한 인터페이스 여러 개가 범용 인터페이스 하나보다 낫다.

## DIP 의존관계 역전 원칙(Dependency Inversion Principle)

- ==**구체화의 의존하면 안되고 추상화에 의존해야 한다.**==
- 클라이언트 입장에서 구현체가 아닌 클래스를 바라보게 해야한다.
- ==_**역할에 의존해야 한다는 말과 같다.**_==

**객체 지향의 핵심은 다형성==_다형성 만으로는 OCP , DIP 를 지킬 수 없다._==**

---

# 객체 지향 설계와 스프링

스프링은 ==**다형성 + OCP, DIP를 가능하게 지원**==

### 스프링 DI, DI컨테이너

- **DI(Dependency Injection)** : 의존관계, 의존성 주입
- DI 컨테이너 제공
- _==**클라이언트 코드의 변경 없이 기능 확장(OCP , DIP 위배하지 않는다)**==_

  

## 정리

- 모든 설계에 _==**역할과 구현을 분리**==_하자.