## 회원 엔티티

- Member(회원) : Address(임베디드 타입, 회원과 배송테이블)
- OrderItem(주문상품) : 실무에서는 OrderLIne, LineItem 으로 많이 표현
    
    ![[Untitled 13.png|Untitled 13.png]]
    
    회원이 주문을 하기 때문에 회원이 주문리스트(orders : List)를 가지는게 맞는거 처럼 보이지만 객체 세계에서는 주문이 회원을 참조하는 것이 맞다.
    

28 46

## 회원 테이블 분석

- MEMBER, DELIVERY 테이블에 CITY,STREET,ZIPCODE 는 Address 임베디드 타입 정보가 그대로 들어가 있다.
- Album, Book, Movie를 통합해서 ITEM 테이블에 들어가있다.
    
    ![[Untitled 1 10.png|Untitled 1 10.png]]
    
    테이블명이 ORDER가 아니고 ORDERS 인 이유는 예약어 order by 때문이다. 관례상 ORDERS를 많이 사용
    

  

## 연관관게 매핑 분석

  

## 엔티티 설계시 주의점

### 엔티티에는 가급적 setter를 사용하지 않는다.

- 변경포인트가 너무 많아서, **==유지보수가 어렵다.==**

  

### 모든 연관관계는 ==_**지연로딩(Lazy)으로 설정!!**_==

- 즉시로딩(’EAGER’)은 예측이 어렵고, 어떤 SQL이 실행될지 추적하기 어렵다.
- ==_**실무에서 모든 연관관게는 지연로딩(’LAZY’) 로 설정해야 한다!!!**_==
- fetch join 을 쓰면된다.
- XXToOne(ManyToOne, OneToOne)은 기본 패치자 EAGER(즉시로딩)으로 되어있기 때문에 지연로딩(LAZY)로 바꿔야한다.(그 외에는 기본이 지연로딩이다)

  

### cascade

- JPA에서는 각각의 엔티티를 persist해줘야 한다. order를 persist를 한다면 orderItem 들과 delivery를 각각 해줘야한다.
- 하지만 CascadeType.ALL을 하면 order만 persist해주면 자동으로 나머지 엔티티들을 persist해준다.
    
    ```java
    @OneToMany(mappedBy = "order", cascade = CascadeType.ALL)
    private List<OrderItem> orderItems = new ArrayList<>();
    
    @OneToOne(fetch = FetchType.LAZY, cascade = CascadeType.ALL)
    @JoinColumn(name = "delivery_id")
    private Delivery delivery;
    ```
    

  

### 연관관계 편의 메서드

- 아래 코드를 예시로 Member가 주문을 한다면 orders에도 주입을 해줘야 할 것 이다.
- Order도 마찬가지다.

```java
public class Member {

    @Id @GeneratedValue
    @Column(name = "MEMBER_ID")
    private Long id;

    private String name;

    @Embedded
    private Address address;

    @OneToMany(mappedBy = "member")
    private List<Order> orders = new ArrayList<>();

}
```